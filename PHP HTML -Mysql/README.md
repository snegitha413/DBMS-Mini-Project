**Access Mysql DB using a simple PHP script**

<ul>
  <li>Create a HTML and PHP file and store it in the "www" folder in "wamp64" folder</li>
  <li>Use HTML as frontend and create a addmission form to get details </li>
  <li>Create a database in the wamp server</li>
  <li>Use PHP as backend to store the user details values into the database</li>
  <li>Create a Virtual host in the wamp server and run it </li>
</ul>
